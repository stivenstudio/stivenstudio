/** @type {import('next').NextConfig} */
const nextConfig = {
	i18n: {
		locales: ['es', 'en'], // List of all supported locales
		defaultLocale: 'es', // Default locale
	}
}

module.exports = nextConfig
